# The-verification-gate-your-data-has-to-pass
Cleaning code can silently fail — a regex that doesn't quite match, a column name typo, an edge case that slips through. This project treats "the data is clean" as a claim that needs proof, not an assumption
